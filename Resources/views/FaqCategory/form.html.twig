{{ form_start(form) }}
{{ form_errors(form) }}

{{ form_rest(form) }}

<div class="form-group">
    <div class="col-sm-10 col-sm-offset-2">
        <button type="submit" id="faq_category_submit" class="btn btn-block btn-success"><i class="fa fa-check"></i> {{ 'validate'|trans }}</button>
    </div>
</div>

{{ form_end(form) }}