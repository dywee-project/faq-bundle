{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_rest(form) }}

<div class="form-group">
    <div class="col-sm-10 col-sm-offset-2">
        <button type="submit" class="btn btn-success btn-block"><i class="fa fa-check"></i> {{ 'validate'|trans }}</button>
    </div>
</div>

{{ form_end(form) }}